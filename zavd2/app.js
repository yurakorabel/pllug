let text = 'yura';
let uppercase = text.toUpperCase();
let first_letter = uppercase[0].toLowerCase();
let rest_of_text = first_letter + uppercase.slice(1);

console.log(rest_of_text);

// працює лише для одного прикладу
// має бути універсальне

